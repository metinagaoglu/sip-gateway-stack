<configuration name="lua.conf" description="LUA Configuration">
  <settings>

    <!-- 
    Specify local directories that will be searched for LUA modules
    These entries will be pre-pended to the LUA_CPATH environment variable
    -->
    <!-- <param name="module-directory" value="/usr/lib/lua/5.1/?.so"/> -->
    <!-- <param name="module-directory" value="/usr/local/lib/lua/5.1/?.so"/> -->

    <!-- 
    Specify local directories that will be searched for LUA scripts
    These entries will be pre-pended to the LUA_PATH environment variable
    -->
    <!-- <param name="script-directory" value="/usr/local/lua/?.lua"/> -->
    <!-- ACIK OLMASI ZORUNLU. Vanilla dosyada bu satir yorumdaydi, yani LUA_PATH
         hic ayarlanmiyordu ve `lua <script>` API'si scriptleri BULAMIYORDU:
             fs_cli -x "lua max_calls.lua ..."  ->  -ERR Cannot execute script
         Dosya /opt/freeswitch/scripts altinda dogru yerde ve okunabilir olsa
         bile bu olur; belirti "script yok" gibi gorunmez, "calistirilamiyor"
         gibi gorunur. Ilginc bir yan detay: `luarun` (asenkron) `+OK` doner
         ama script gercekte HIC calismaz — yani basarili gorunen bir cikti
         yaniltici olabilir. $${script_dir} = /opt/freeswitch/scripts. -->
    <param name="script-directory" value="$${script_dir}/?.lua"/>

    <!--<param name="xml-handler-script" value="/dp.lua"/>-->
    <!--<param name="xml-handler-bindings" value="dialplan"/>-->

    <!--
	The following options identifies a lua script that is launched
	at startup and may live forever in the background.
	You can define multiple lines, one for each script you 
	need to run.
    -->
    <!--<param name="startup-script" value="startup_script_1.lua"/>-->
    <!--<param name="startup-script" value="startup_script_2.lua"/>-->
    
    <!--<hook event="CUSTOM" subclass="conference::maintenance" script="catch-event.lua"/>-->
  </settings>
</configuration>
