# Devir notu — 2026-08-12

Bu dosya, calisan yigini devralacak yeni bir oturum icin yazildi.
**Onceki devir notu (2026-08-09) artik gecersiz** — orada anlatilan "cagri
kurulmuyor" blokeri ve SIGSEGV supheleri COZULDU.

## Durum: TAMAMLANDI ve main'e merge edildi

Dal: `main` (merge commit `222ec80`, sonrasinda `b0ac39d` dispatcher fix'i)
`feature/pgsql-stack-rebuild` dali silinmedi, main artik onun onunde.

```bash
./scripts/verify-all.sh     # PASS=17 FAIL=0, ~4 dk
```

Bu komut yiginin tamamini dogrular ve sonunda kapsayici saglik + FreeSWITCH
RestartCount ozeti basar. **Ilk isiniz bu olsun**; her sey yesilse yigin
saglamdir.

## Uctan uca KANITLANMIS olanlar

Hepsi otomatik test altinda (`scripts/verify-*.sh`), elle degil:

| Islev | Kanit |
|---|---|
| Digest kayit (Kamailio, PostgreSQL `subscriber`) | verify-04 |
| FreeSWITCH core DB'si PostgreSQL'de | verify-06 |
| tenant-aware directory/dialplan (`xml_curl`) | verify-07 |
| echo (9999) + gercek RTP echo olcumu | verify-08 |
| alice -> bob, cift yonlu RTP, dongu yok (2 kanal) | verify-09 |
| CDR PostgreSQL'e yaziliyor, alanlar anlamli | verify-10 |
| Es zamanli cagri siniri (`max_calls`) -> 486 Busy Here | verify-11 |
| Trunk YAPISAL (profil, gateway, numara deseni) | verify-12 |
| Sema/FK/index/seed `ha1` dogrulugu | verify-90 |
| Kiraci izolasyonu: capraz sifre REDDEDILIYOR | verify-91 |
| Sifirdan sema kurulumu (gecici DB'de) | verify-92 |
| 5 ardisik cagri, kaynak sizintisi yok, 9998 playback | verify-93 |
| Kamailio FreeSWITCH'ten once baslayabiliyor | verify-94 |

Zoiper ile elle de dogrulandi: kayit, 9999 echo (ses duzgun), cagri kapatma
aninda kapaniyor.

## KANITLANMAYANLAR (dikkat)

- **Gercek SIP trunk.** Saglayici secilmedi; `TRUNK_ENABLED=false`.
  verify-12 kapali modda YAPISAL dogrulama yapar. Gercek register, giden
  cagri ve ses HIC test edilmedi.
- **Task 13 (gelen trunk cagrisi)** yazilmadi: `public` dialplan baglami ve
  gelen cagri ACL'i yok.
- **Ses KALITESI** (jitter, kopma) otomatik olculmuyor.
- TLS/SRTP, voicemail, IVR kapsam disi.

## Siradaki isler (oncelik sirasiyla)

1. **SIP trunk** — kullanici "bir sonraki asama" dedi. `.env`'de `TRUNK_*`
   doldurulup `TRUNK_ENABLED=true`, sonra
   `docker compose up -d --build freeswitch && ./scripts/verify-12-trunk-out.sh`.
   Ardindan Task 13 (gelen cagri).
2. **Yonetim arayuzu** — kullanici AYRI bir conversation'da yapmak istiyor.
   Karar gerekcesi: genel FreeSWITCH GUI'de acik YOK (FusionPBX olgun ve
   baskin), ama bu yiginin Kamailio+FreeSWITCH AYRIK mimarisi FusionPBX
   tarafindan yonetilmiyor. Kendi kullanim icin basit panel mantikli ve ucuz
   (`xmlapi` Flask uzerine oturur: kullanici CRUD, `ul.dump` kayit durumu,
   canli cagrilar, CDR listesi). Urunlestirme karari trunk gercek kullanima
   girdikten sonra verilmeli.
3. **Kalan test borclari** (`progress.md` sonunda listeli): diger verify
   scriptlerinde `$FSCLI ... | grep -q` + `pipefail` deseni kirilgan;
   FreeSWITCH icin "calisan konfigurasyon == repo" kontrolu yok; `trusted`
   ACL `172.16.0.0/12`'nin tamamina izin veriyor.

## Yeni oturumun MUTLAKA bilmesi gerekenler

`README.md` icindeki **"Bilinen tuzaklar"** bolumunu okuyun. Bu oturumda
bulunan 15+ hatanin cogu "yapilandirma dosyasi dogru gorunuyor ama canli
deger farkli" sinifindaydi. Ozetle:

- `kamailio.cfg` ve `freeswitch/entrypoint.sh` **image'a gomulu**;
  `restart` degisikligi ALMAZ, `up -d --build` gerekir.
- FreeSWITCH global degiskenleri (`$${...}`) **baslangic sirasina bagli**;
  yapilandirma degeri baglamayin, `.env`'den sablona (`*.tmpl`) gomun.
- Yeni bir `.env` degiskeni **UC yerde** tanimlanmali: `.env(.example)`,
  `entrypoint.sh` icindeki `VARS`, ve compose `environment`.
- Sema degisikligi **`db/init/` scriptlerine** yazilmali; initdb yalnizca bos
  veri dizininde kosar. `verify-92` bunu gecici DB'de dogrular.
- `git checkout` ile dal degistirdikten sonra **`up -d --force-recreate`**
  gerekir; checkout bind-mount'u koparir.
- Dogrulamada **`xml_locate`/`sofia status`** kullanin, dosyayi okumayin —
  canli deger dosyadan farkli olabilir.

## Yontem notu

Superpowers **subagent-driven-development** kullanildi; ledger asil kayittir:
`.superpowers/sdd/2026-08-08-voip-stack-pgsql-rebuild/progress.md`
**Bu dizin `.gitignore`'da**, yani ledger repo'ya girmez ve yalnizca bu
makinede bulunur. Ayrintili gerekceler, olculen kanitlar ve elenen
hipotezler oradadir; bu dosya onun repo'ya girebilen ozetidir.
Her task icin plan hatalari, olculen kanitlar ve elenen hipotezler orada.
Plan (`docs/superpowers/plans/`) kanitla yazildi ama her satiri
calistirilmadi; inceleme dongusu 20'den fazla gercek hata yakaladi.
**Plana koruyucu bir supheyle yaklasin** ve her iddiayi olcun.
